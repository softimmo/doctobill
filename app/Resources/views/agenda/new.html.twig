{% extends 'base.html.twig' %}

{% block body %}
    <fieldset>
        <legend>
    <h1>Agenda creation</h1>
        </legend>
    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}
    </fieldset>

{% endblock %}
