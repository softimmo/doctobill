{% extends 'base.html.twig' %}
{% form_theme form 'form/form_div_6.html.twig' %}

{% block body %}
    <fieldset>
        <legend>
            <h1>Création {% trans%}identification{% endtrans%}</h1>
        </legend>
        {{ form_start(form) }}
        {{ form_widget(form) }}
        <hr/>
        <div class="col-sm-12 text-center">
            <input type="submit" value="Valider" class="btn btn-lg btn-success"/>
        </div>
        {{ form_end(form) }}
    </fieldset>
{% endblock %}
