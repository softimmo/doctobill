<!--  semaine:_form.semaine.htm.twig -->
 {{ form_errors(form) }}
{{ form_start(form) }}
<div class="col-sm-3">{{ form_label(form.dateDebut) }}</div>
<div class="col-sm-2">{{ form_widget(form.dateDebut) }}
    <span class="error-inline">{{ form_errors(form.dateDebut) }}</span>
</div>
<div class="col-sm-12  text-center" style="margin:10px">
    <input type="submit" value="Sauvegarder" class="btn btn-success btn-lg"/>
    {{ form_end(form) }}
</div>
